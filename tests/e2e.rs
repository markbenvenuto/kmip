/*
Make these true end to end tests

Need to start the client and server and verify they all work

See https://rust-cli.github.io/book/tutorial/testing.html

https://github.com/droundy/roundqueue/blob/master/tests/lib.rs
*/
//extern crate kmip_client;
//extern crate main;

#[test]
fn test_create_request3() {
    // let bytes = vec![
    //     0x42, 0x00, 0x78];

    // let store = Arc::new(KmipMemoryStore::new());

    // let server_context = ServerContext::new(store);

    // let mut rc = RequestContext::new(&server_context);
    // process_kmip_request(&mut rc, bytes.as_slice());

    // process_kmip_request(&mut rc, get_bytes.as_slice());

    println!("Hello");
}